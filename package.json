{
  "private": true,
  "name": "serein-docs",
  "version": "1.0.0",
  "repository": "https://gitlab.vipklm.com/serein/serein-cloud-docs",
  "author": "serein <fengyp8963@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "vitepress dev --host='0.0.0.0' --port=3000",
    "build": "vitepress build",
    "serve": "vitepress serve --port=5000",
    "pages": "npm run build && bash scripts/deploy-gh.sh"
  },
  "dependencies": {
    "vitepress": "0.22.3"
  },
  "devDependencies": {
    "@vuepress/plugin-search": "^1.9.7",
    "stylus": "^0.57.0",
    "stylus-loader": "^6.2.0"
  }
}
